// Prevent us from attempting to use variables
// that are not declared
/*
"use strict"
// Create an array with different data types
let mixedArray = [42, "hello", true];
console.log(mixedArray); // Log the array

// Log the typeof the array
console.log(typeof mixedArray); // Output: "object"

// Remove an item from the array
mixedArray.shift();
console.log(mixedArray); // Log the array after removing the first item

// Add an item to the array
mixedArray.push("new item");
console.log(mixedArray); // Log the array after adding an item

// Change the value of an element in the array
mixedArray[1] = false;
console.log(mixedArray); // Log the array after changing an element
*/
